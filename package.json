{
  "name": "@primeuicom/skills-marketplace",
  "version": "0.1.3",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm run validate",
    "format": "prettier --write . --ignore-unknown",
    "format:check": "prettier --check . --ignore-unknown",
    "lint": "pnpm run validate",
    "release:check": "node ./scripts/verify-release-version.mjs",
    "release:prepare": "node ./scripts/set-version.mjs",
    "sync:manifest-version": "node ./scripts/sync-manifest-version.mjs",
    "test": "pnpm run validate",
    "typecheck": "pnpm run validate",
    "validate": "node ./scripts/validate-marketplace.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
